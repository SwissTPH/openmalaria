# I (DH) have been using the repository via git-svn, hence this .gitignore file.
html
profiling
.kdev4
openmalaria.kdev4
# These two folders are tracked in svn anyway (git won't track folders).
util/SchemaTranslator/scenarios
util/SchemaTranslator/translatedScenarios
util/.metadata
util/SchemaTranslator/*.class
.sconsign.dblite
build
rel-build
lib
test/compareOutputsFloat.pyc
